// import './MoreButton.css';

// const MoreButton = ({
//   isLoading,
//   loadingError,
//   idolListLength,
//   handleMoreBtn,
//   nextCursor,
// }) => {
//   return (
//     <div className="more-button">
//       {isLoading && idolListLength > 0 ? (
//         <button disabled>Loading...</button>
//       ) : (
//         !loadingError &&
//         nextCursor && <button onClick={handleMoreBtn}>더보기</button>
//       )}
//     </div>
//   );
// };

// export default MoreButton;
